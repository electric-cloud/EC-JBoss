<editor>
    <help>/commander/pages/@PLUGIN_NAME@/@PLUGIN_KEY@_help?s=Administration&amp;ss=Plugins#StartHostController</help>
    <formElement>
        <type>entry</type>
        <label>Configuration name:</label>
        <property>serverconfig</property>
        <required>1</required>
        <documentation>Name of the configuration to be used. URL and credentials are retrieved from the given configuration. To view or create a new configuration, go to the Administration -> Plugins tab, and select 'Configure' action for @PLUGIN_KEY@ plugin.</documentation>
        <propertyReference>/plugins/@PLUGIN_NAME@/project/jboss_cfgs</propertyReference>
        <htmlDocumentation>Name of the configuration to be used. URL and credentials are retrieved from the given configuration.<br/><br/>A Configuration defines connection details and can be created by going to plugin <a style="text-decoration: none !important; border-bottom-style: dashed; border-bottom-width: thin; font-size: inherit; color: inherit; font-family: inherit; border-color: #d8d8d8; border-spacing: 2px;" target="_blank" href="/commander/pages/@PLUGIN_NAME@/configurations">configuration page</a>.</htmlDocumentation>
    </formElement>
	<formElement>
        <type>entry</type>
        <label>Startup Scipt:</label>
        <property>startupScript</property>
        <required>1</required>
        <documentation>Provide the absolute physical path of the domain script, i.e: 'domain.bat', '/usr/tools/bin/domain.sh', 'c:/Program Files/JBoss/bin/domain.bat'.</documentation>
    </formElement>
    <formElement>
        <type>entry</type>
        <label>Domain Config:</label>
        <property>domainConfig</property>
        <required>0</required>
        <documentation>For domain controllers only. Name of the domain configuration file that ElectricFlow uses to start the domain controller with a different domain configuration (--domain-config). By default domain.xml is used.</documentation>
    </formElement>
    <formElement>
        <type>entry</type>
        <label>Host Config:</label>
        <property>hostConfig</property>
        <required>0</required>
        <documentation>For domain and host controllers. Name of the host configuration file that ElectricFlow uses to start the domain or host controller with a different host configuration (--host-config). By default host.xml is used.</documentation>
    </formElement>
    <formElement>
        <type>entry</type>
        <label>Host Name:</label>
        <property>jbossHostName</property>
        <required>0</required>
        <documentation>Host name for verification via master CLI that host controller is started</documentation>
    </formElement>
    <formElement>
        <type>entry</type>
        <label>Additional Options:</label>
        <property>additionalOptions</property>
        <required>0</required>
        <documentation>Additional options</documentation>
    </formElement>
</editor>

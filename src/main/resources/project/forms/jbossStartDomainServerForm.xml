<editor>
    <help>/commander/pages/@PLUGIN_NAME@/@PLUGIN_KEY@_help?s=Administration&amp;ss=Plugins#StartDomainServer</help>
    <formElement>
        <type>entry</type>
        <label>Configuration name:</label>
        <!-- Improvements for CEV-18687 -->
        <configuration>1</configuration>
        <!-- End of improvements for CEV-18687 -->
        <property>serverconfig</property>
        <required>1</required>
        <documentation>Name of the configuration to be used. URL and credentials are retrieved from the given configuration. To view or create a new configuration, go to the Administration -> Plugins tab, and select 'Configure' action for @PLUGIN_KEY@ plugin.</documentation>
        <propertyReference>/plugins/@PLUGIN_NAME@/project/jboss_cfgs</propertyReference>
        <htmlDocumentation>Name of the configuration to be used. URL and credentials are retrieved from the given configuration.<br/><br/>A Configuration defines connection details and can be created by going to plugin <a style="text-decoration: none !important; border-bottom-style: dashed; border-bottom-width: thin; font-size: inherit; color: inherit; font-family: inherit; border-color: #d8d8d8; border-spacing: 2px;" target="_blank" href="/commander/pages/@PLUGIN_NAME@/configurations">configuration page</a>.</htmlDocumentation>
    </formElement>
	<formElement>
        <type>entry</type>
        <label>Physical location of the domain script:</label>
        <property>scriptphysicalpath</property>
        <required>1</required>
        <documentation>Provide the absolute physical path of the domain script, i.e: 'domain.bat', '/usr/tools/bin/domain.sh', 'c:/Program Files/JBoss/bin/domain.bat'.</documentation>
    </formElement>
    <formElement>
        <type>entry</type>
        <label>Optional domain configuration name:</label>
        <property>alternatejbossconfig</property>
        <required>0</required>
        <documentation>For domain controllers only. Name of the domain configuration file that CloudBees Flow uses to start the domain controller with a different domain configuration (--domain-config). By default domain.xml is used.</documentation>
    </formElement>
    <formElement>
        <type>entry</type>
        <label>Optional host configuration name:</label>
        <property>alternateJBossConfigHost</property>
        <required>0</required>
        <documentation>For domain and host controllers. Name of the host configuration file that CloudBees Flow uses to start the domain or host controller with a different host configuration (--host-config). By default host.xml is used.</documentation>
    </formElement>
</editor>
